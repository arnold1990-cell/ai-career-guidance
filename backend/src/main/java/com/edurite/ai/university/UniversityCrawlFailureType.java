package com.edurite.ai.university;

public enum UniversityCrawlFailureType {
    TIMEOUT,
    NOT_FOUND,
    FORBIDDEN,
    PROTECTED,
    NETWORK_ERROR,
    SSL_ERROR,
    NO_RESULT,
    THIN_CONTENT,
    IRRELEVANT_CONTENT,
    ACCESS_DENIED,
    EMPTY_CONTENT,
    FETCH_ERROR
}
