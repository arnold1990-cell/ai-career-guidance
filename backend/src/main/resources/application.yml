spring:
  application:
    name: edurite-backend

  profiles:
    active: dev

  datasource:
    url: jdbc:postgresql://localhost:5432/edurite
    username: postgres
    password: Crt@Ailsa

  data:
    redis:
      host: localhost
      port: 6379
      repositories:
        enabled: false

  cache:
    type: redis

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate.jdbc.time_zone: UTC

  flyway:
    enabled: true
    locations: classpath:db/migration

security:
  jwt:
    secret: 8f4c2b9d1e7a5c3f8b6a0d2e4f9c1b7a6e3d5f8a2c4b6e9d1f7a3c5b8e0d2f4
    access-token-expiration: 3600
    refresh-token-expiration: 604800

server:
  port: 8080

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

gemini:
  api-key: AIzaSyCo5MrVWRexb_hyec_74O_xIpG3uRfh0A4
  model: gemini-2.5-flash
  base-url: https://generativelanguage.googleapis.com