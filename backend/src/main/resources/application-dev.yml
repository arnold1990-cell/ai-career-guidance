spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/edurite
    username: postgres
    password: Crt@Ailsa

  data:
    redis:
      host: localhost
      port: 6379

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080