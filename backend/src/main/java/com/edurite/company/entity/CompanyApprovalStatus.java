package com.edurite.company.entity; // declares the package path for this Java file

/**
 * This enum named CompanyApprovalStatus is part of the Spring Boot application.
 * It groups related logic so the project stays organized and easier to learn.
 */
public enum CompanyApprovalStatus { // defines a fixed set of constant values
    PENDING, // supports the surrounding application logic
    APPROVED, // supports the surrounding application logic
    REJECTED, // supports the surrounding application logic
    MORE_INFO_REQUIRED // supports the surrounding application logic
} // ends the current code block
