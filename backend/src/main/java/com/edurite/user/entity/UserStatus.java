package com.edurite.user.entity; // declares the package path for this Java file

/**
 * This enum named UserStatus is part of the Spring Boot application.
 * It groups related logic so the project stays organized and easier to learn.
 */
public enum UserStatus { // defines a fixed set of constant values
    ACTIVE, // supports the surrounding application logic
    PENDING, // supports the surrounding application logic
    SUSPENDED // supports the surrounding application logic
} // ends the current code block
