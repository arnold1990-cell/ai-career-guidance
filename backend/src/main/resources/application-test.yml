spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/edurite_test
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080